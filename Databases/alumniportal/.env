# When sslmode is disabled, the PostgreSQL client connects to the database over a plain TCP connection without encryption
# Local Environment Trust:
#   When you run PostgreSQL locally, the connection is on localhost and is inherently considered secure by being inside your machine. Encrypting local connections is usually unnecessary.

DATABASE_URL=postgres://postgres:Radha%4077@localhost:5432/alumni_portal?sslmode=disable